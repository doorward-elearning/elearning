apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: edudoor-data
  namespace: basil-edudoor
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: databases
  resources:
    requests:
      storage: 10Gi

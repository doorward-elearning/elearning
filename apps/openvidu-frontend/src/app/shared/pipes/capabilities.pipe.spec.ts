import { CapabilitiesPipe } from './capabilities.pipe';

describe('CapabilitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new CapabilitiesPipe();
    expect(pipe).toBeTruthy();
  });
});

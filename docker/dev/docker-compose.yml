version: '3.3'
services:
  edudoor_frontend:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    volumes:
      - /usr/app/node_modules
      - ../../:/usr/app
    ports:
      - '3005:3000'
    expose:
      - '3000'
    command:
      - yarn
      - start

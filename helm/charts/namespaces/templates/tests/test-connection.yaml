apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespaces.fullname" . }}-test-connection"
  labels:
{{ include "namespaces.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "namespaces.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

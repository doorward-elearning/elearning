apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/issuer: {{ include "swagat.fullname" . -}}-letsencrypt-prod
  name: {{ include "swagat.fullname" . }}
  namespace: {{ required "A valid foo is required!" .Values.customer -}}-edudoor
  labels:
    {{- include "swagat.labels" . | nindent 4 }}

spec:
  rules:
    - host: {{ include "swagat.public_website_name" . }}
      http:
        paths:
          - backend:
              serviceName: {{ include "swagat.fullname" . }}
              servicePort: 80
            path: /
  tls:
  - hosts:
    - {{ include "swagat.public_website_name" . }}
    secretName: {{ include "swagat.public_website_name" . }}

    # This section is only required if TLS is to be enabled for the Ingress
#  tls:
#      - hosts:
#          - {{ .Values.customer -}}.edudoor.org
#        secretName: example-tls

apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/issuer: {{ include "samvadam.fullname" . -}}-letsencrypt-prod
  name: {{ include "samvadam.fullname" . }}
  namespace: {{ required "A valid foo is required!" .Values.customer -}}-edudoor
  labels:
    {{- include "samvadam.labels" . | nindent 4 }}

spec:
  rules:
    - host: {{ include "samvadam.samvadampublicurl" . }}
      http:
        paths:
          - backend:
              serviceName: {{ include "samvadam.fullname" . }}
              servicePort: 443
#            path: /{{ .Values.customer -}}/{{ .Chart.Name }}
  tls:
  - hosts:
    - {{ include "samvadam.samvadampublicurl" . }}
    secretName: {{ include "samvadam.samvadampublicurl" . }}
    # This section is only required if TLS is to be enabled for the Ingress
#  tls:
#      - hosts:
#          - {{ .Values.customer -}}.edudoor.org
#        secretName: example-tls

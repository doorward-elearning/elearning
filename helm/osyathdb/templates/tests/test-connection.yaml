apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osyathdb.fullname" . }}-test-connection"
  labels:
{{ include "osyathdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "osyathdb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

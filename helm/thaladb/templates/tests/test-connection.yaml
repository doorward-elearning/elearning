apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thaladb.fullname" . }}-test-connection"
  labels:
{{ include "thaladb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "thaladb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

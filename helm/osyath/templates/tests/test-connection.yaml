apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osyath.fullname" . }}-test-connection"
  labels:
{{ include "osyath.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "osyath.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

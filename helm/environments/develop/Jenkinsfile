pipeline {
  agent any

  stages {
    stage('Deploy') {
        steps {
           sh 'cd ${WORKSPACE}/helm/environments/develop && helmfile -l stage=2 delete && sleep 50 && helmfile -l stage=2 sync'
        }
    }
  }

}
